const path = require('path');

// Enable Atlas Autoloading.
const autoloader = require(path.join(__dirname, '/../../autoload'));
const atlas_paths = require('atlas_paths');

////////////////////////////////////////


